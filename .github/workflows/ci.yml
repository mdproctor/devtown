name: Build and Test

on:
  repository_dispatch:
    types: [upstream-published]
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - uses: actions/checkout@v4

      - name: Checkout casehub-pages (sibling)
        uses: actions/checkout@v4
        with:
          repository: casehubio/casehub-pages
          token: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          path: .sibling-pages

      - name: Symlink pages as sibling
        run: ln -s "$GITHUB_WORKSPACE/.sibling-pages" ../pages

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'

      - name: Build pages packages
        working-directory: .sibling-pages
        run: |
          corepack enable
          yarn install --immutable
          yarn build
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GH_PAT }}

      - name: Build frontend
        working-directory: app/src/main/webui
        run: npm install && npm run build
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GH_PAT }}

      - name: Build and test
        run: mvn --batch-mode verify
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
